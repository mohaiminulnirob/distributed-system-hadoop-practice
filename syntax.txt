StringTokenizer tokenizer = new StringTokenizer(line);

String department = tokenizer.nextToken(); // IT
String salary = tokenizer.nextToken();     // 40000

int salaryInt = Integer.parseInt(salary);

IntWritable salaryWritable = new IntWritable(salaryInt);

context.write(new Text(department), salaryWritable);